import { UpdateReposDirective } from './update-repos.directive';

describe('UpdateReposDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateReposDirective();
    expect(directive).toBeTruthy();
  });
});
